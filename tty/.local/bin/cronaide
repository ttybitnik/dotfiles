#!/bin/sh

# Cron/timer wrapper to log and manage aide updates.

: "${ROOT_CMD:=sudo}"

# OPT(set): x=xtrace
set -x

# OPT(aide): u=update
"$ROOT_CMD" aide -u
"$ROOT_CMD" mv /var/lib/aide/aide.db.new.gz /var/lib/aide/aide.db.gz
