#!/usr/bin/env bash

# Standard for logging commands (ISO 8601 and POSIX).

timestamp=$(date +%Y%m%dT%H%M%S)

{ printf ">> %s\n" "$*"; time -p "$@"; } 2>&1 \
    | tee "${HOME}/.orpheus/ttylogs/${timestamp}_$1.log"
